package com.plo.chipApp.controls.signals
{
	import org.osflash.signals.Signal;
	
	public class StartupSignal extends Signal
	{
		public function StartupSignal(...parameters)
		{
			super(parameters);
		}
	}
}